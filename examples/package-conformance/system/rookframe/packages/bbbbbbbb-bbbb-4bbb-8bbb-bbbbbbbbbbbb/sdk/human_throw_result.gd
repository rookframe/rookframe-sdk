## Generated by Rookframe SDK 0.16.0. Regenerate with `rookframe facade`.
extends "res://rookframe/packages/bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb/sdk/operation_result.gd"

const DiceTerm = preload("res://rookframe/packages/bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb/sdk/dice_term.gd")
const DiceTermResult = preload("res://rookframe/packages/bbbbbbbb-bbbb-4bbb-8bbb-bbbbbbbbbbbb/sdk/dice_term_result.gd")
var request_id: String
var participant_id: String
## One of pending, rolled, or cancelled.
var status: String
## Immutable requested terms, available in every successful snapshot.
var plan: Array[DiceTerm] = []
## Raw named results when status is rolled; otherwise empty.
var terms: Array[DiceTermResult] = []
## Sequence of Rookframe's built-in raw Roll report, or zero before a Roll.
var sequence: int = 0
func _init(result: Dictionary) -> void:
	super(result)
	if ok:
		request_id = result.value.get("request_id", "")
		participant_id = result.value.get("participant_id", "")
		status = result.value.get("status", "")
		for term in result.value.get("plan", []):
			plan.append(DiceTerm.new(term.get("name", ""), term.get("faces", 0), term.get("count", 0)))
		for term in result.value.get("terms", []):
			terms.append(DiceTermResult.new(term))
		sequence = result.value.get("sequence", 0)
